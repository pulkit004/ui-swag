import SwaggerInput from 'renderer/components/swaggerInput';

const Analyse = () => {
  return (
    <div className="grid grid-cols-12">
      <SwaggerInput onConvert={() => {}} />
    </div>
  );
};

export default Analyse;
